<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="output.example.contact_app.repository.ContactRepository">

    <select id="findAllOrderByContactDateAsc" resultType="output.example.contact_app.data.ContactLog">
        SELECT id, contact_date, created_at, updated_at
        FROM contact_log
        ORDER BY contact_date ASC
    </select>

    <select id="findById" resultType="output.example.contact_app.data.ContactLog">
        SELECT id, contact_date, created_at, updated_at
        FROM contact_log
        WHERE id = #{id}
    </select>

    <select id="findByContactDate" resultType="output.example.contact_app.data.ContactLog">
        SELECT id, contact_date, created_at, updated_at
        FROM contact_log
        WHERE contact_date = #{contactDate}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO contact_log (contact_date)
        VALUES (#{contactDate})
    </insert>

    <update id="update">
        UPDATE contact_log
        SET contact_date = #{contactDate}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM contact_log
        WHERE id = #{id}
    </delete>

    <select id="findConsecutiveLog" resultType="output.example.contact_app.data.ContactLog">
        SELECT id, contact_date, created_at, updated_at
        FROM contact_log
        WHERE contact_date &lt;= #{today}
        ORDER BY contact_date DESC
    </select>

</mapper>